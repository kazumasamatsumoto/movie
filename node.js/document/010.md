# #010 「Libuv - Node.jsの心臓部」

```javascript
// loop-phases.js
setTimeout(() => console.log('timer phase'), 0);
setImmediate(() => console.log('check phase'));
process.nextTick(() => console.log('microtask phase'));
```

```javascript
// thread-pool-io.js
import { readFile } from 'node:fs';
readFile('package.json', () => console.log('fs done'));
import dns from 'node:dns';
dns.lookup('example.com', () => console.log('dns lookup done'));
```

```javascript
// uv-config.js
console.log(`Thread pool size: ${process.env.UV_THREADPOOL_SIZE ?? 4}`);
console.log(`Platform: ${process.platform}`);
console.log(`Libuv version: ${process.versions.uv}`);
```
