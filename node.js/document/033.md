# #033 「ページングの仕組み」

```bash
getconf PAGESIZE
```

```bash
vm_stat | grep 'Pages'
```

```text
ページフォールトは `dtrace -n 'syscall::page_fault:return { printf("fault\n"); }'` で観測できます。
```

```javascript
// touch-pages.mjs
const page = Buffer.alloc(4096);
for (let i = 0; i < page.length; i += 4096) {
  page[i] = 0xff;
}
console.log('touched page boundaries');
```
