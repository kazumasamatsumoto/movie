# #008 「`Pick`と`Omit`だけでDTOを組み立てた末路」制作ドキュメント

## 台本
ずんだもん「`Pick`と`Omit`だけでDTOを組み立てた末路って絶対あるよね？」
四国めたん「わかります。気づいたら型がジグソーパズルみたいにバラバラになりますからね」
ずんだもん「しかも“差分どれ？”って聞かれてソース掘りながら沈黙するの、地味に恥ずかしいんだよ！」
四国めたん「それならTypeScriptのユーティリティ型を組み合わせて、DTO用の差分型を最初から用意しておくと整理できます」
ずんだもん「いや待って、それ作ったら“共通化して”ってさらに要求されるやつ！」
四国めたん「共感した方は“ユーティリティ職人”ってコメントで教えてください！」
ずんだもん「あなたのTypeScriptあるあるも募集中だよ！」

## 画面表示用コード

```typescript
type User = {
  id: string;
  name: string;
  role: "admin" | "editor";
};

type CreateUserDto = Pick<User, "name" | "role">;
type UpdateUserDto = Partial<CreateUserDto>;

function applyUpdate(user: User, update: UpdateUserDto): User {
  return { ...user, ...update };
}

const next = applyUpdate({ id: "1", name: "M", role: "editor" }, { role: "admin" });
```

## 制作メモ
- **想定シーン**: DTOをユーティリティ型だけで量産したら依存が複雑化してしまった瞬間
- **狙いトーン**: 「便利だけど沼」という矛盾を笑いに変える
- **技術キーワード**: `Pick`, `Omit`, `Partial`, DTO
- **使いたい一言**: 「ユーティリティ職人」
- **CTA案**: コメントで「DTOどう分割してる？」と設計談義を促す
- **備考**: コードは`Pick`→`Partial`→実装関数の三段で、整理された形を提示して安心感を残す
