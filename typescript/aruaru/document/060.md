# #060 「`any`禁止Lintが解決策を奪っていく」制作ドキュメント

## 台本
ずんだもん「TypeScript書いてるとさ、`any`禁止Lintが解決策を奪っていくって絶対あるよね？」
四国めたん「わかります。`any`で逃げた翌日にレビューが真っ赤になりますからね」
ずんだもん「しかもそのまま`any`禁止Lintが解決策を奪っていく状態になって、チーム全員が固まるんだよ！」
四国めたん「そこでTypeScriptの`unknown` + `satisfies`コンボを使えば、`unknown`で受けて`satisfies`で構造を固定すると安心です」
ずんだもん「いや待って、それやると`any`禁止Lintが解決策を奪っていく第2ラウンドが始まるやつ！」
四国めたん「共感した方は“any卒業宣言”ってコメントで教えてください！」
ずんだもん「あなたのTypeScriptあるあるも募集中だよ！」

## 画面表示用コード

```typescript
function fetchLegacy(): any {
  return { id: "u1", role: "admin" };
}

const rawUser = fetchLegacy();

type StrictUser = {
  id: string;
  role: "admin" | "editor";
};

const normalized = {
  id: rawUser.id,
  role: rawUser.role ?? "editor",
} satisfies StrictUser;

function ensureStrict(user: StrictUser) {
  return `${user.id}:${user.role}`;
}

console.log(ensureStrict(normalized));
```

## 制作メモ
- **想定シーン**: `any`禁止Lintが解決策を奪っていくに遭遇した瞬間を切り取るネタ
- **狙いトーン**: 自虐多めでレビュー戦線を語る
- **技術キーワード**: `unknown`, `satisfies`, `any`
- **使いたい一言**: 「any卒業宣言」
- **CTA案**: コメントで「any卒業宣言」と書いてもらい、同じ経験を募る
- **備考**: レガシーAPIや暫定対応の文脈で語ると共感を得やすい。
