# #044 「`Result`型を導入したらハッピーなのか問題」制作ドキュメント

## 台本
ずんだもん「TypeScript書いてるとさ、`Result`型を導入したらハッピーなのか問題って絶対あるよね？」
四国めたん「わかります。Result型を導入したら関数定義が長くなりがちですよね」
ずんだもん「しかもそのまま`Result`型を導入したらハッピーなのか問題状態になって、チーム全員が固まるんだよ！」
四国めたん「そこでTypeScriptのResult型パターンを使えば、ジェネリクスと判別ユニオンで安全な結果型を扱えます」
ずんだもん「いや待って、それやると`Result`型を導入したらハッピーなのか問題第2ラウンドが始まるやつ！」
四国めたん「共感した方は“Result派”ってコメントで教えてください！」
ずんだもん「あなたのTypeScriptあるあるも募集中だよ！」

## 画面表示用コード

```typescript
type Result<T, E> = { ok: true; value: T } | { ok: false; error: E };

function success<T>(value: T): Result<T, never> {
  return { ok: true, value };
}

function failure<E>(error: E): Result<never, E> {
  return { ok: false, error };
}

function unwrap<T, E>(result: Result<T, E>): T {
  if (!result.ok) {
    throw result.error;
  }
  return result.value;
}

unwrap(success(42));
```

## 制作メモ
- **想定シーン**: `Result`型を導入したらハッピーなのか問題に遭遇した瞬間を切り取るネタ
- **狙いトーン**: 関数戻り値で盛り上がる
- **技術キーワード**: Result型, 判別ユニオン
- **使いたい一言**: 「Result派」
- **CTA案**: コメントで「Result派」と書いてもらい、同じ経験を募る
- **備考**: エラーハンドリング改善ネタとして語ると効果的。
