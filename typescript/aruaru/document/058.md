# #058 「`DeepPartial`を作りたくなるタイミング」制作ドキュメント

## 台本
ずんだもん「TypeScript書いてるとさ、`DeepPartial`を作りたくなるタイミングって絶対あるよね？」
四国めたん「わかります。`Pick`と`Omit`でパーツを組むと脳が絡まりますよね」
ずんだもん「しかもそのまま`DeepPartial`を作りたくなるタイミング状態になって、チーム全員が固まるんだよ！」
四国めたん「そこでTypeScriptのユーティリティ型の組み合わせを使えば、`Partial`と組み合わせて差分型を最初から用意すると整理できます」
ずんだもん「いや待って、それやると`DeepPartial`を作りたくなるタイミング第2ラウンドが始まるやつ！」
四国めたん「共感した方は“ユーティリティ職人”ってコメントで教えてください！」
ずんだもん「あなたのTypeScriptあるあるも募集中だよ！」

## 画面表示用コード

```typescript
type User = {
  id: string;
  name: string;
  role: "admin" | "editor";
};

type CreateUserDto = Pick<User, "name" | "role">;
type UpdateUserDto = Partial<CreateUserDto>;

function applyUpdate(user: User, update: UpdateUserDto): User {
  return { ...user, ...update };
}

const next = applyUpdate({ id: "1", name: "M", role: "editor" }, { role: "admin" });
```

## 制作メモ
- **想定シーン**: `DeepPartial`を作りたくなるタイミングに遭遇した瞬間を切り取るネタ
- **狙いトーン**: 組み立て作業を笑いに変える
- **技術キーワード**: `Pick`, `Omit`, `Partial`, DTO
- **使いたい一言**: 「ユーティリティ職人」
- **CTA案**: コメントで「ユーティリティ職人」と書いてもらい、同じ経験を募る
- **備考**: フォーム更新やDTO差分を例に出すとリアルです。
