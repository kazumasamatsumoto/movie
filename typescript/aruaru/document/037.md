# #037 「`useState<string | null>`って本当に必要？」制作ドキュメント

## 台本
ずんだもん「TypeScript書いてるとさ、`useState<string | null>`って本当に必要？って絶対あるよね？」
四国めたん「わかります。`useState<string | null>`って書くか悩みますよね」
ずんだもん「しかもそのまま`useState<string | null>`って本当に必要？状態になって、チーム全員が固まるんだよ！」
四国めたん「そこでTypeScriptのReact型の型注釈を使えば、初期値とジェネリクス指定で推論を合わせられます」
ずんだもん「いや待って、それやると`useState<string | null>`って本当に必要？第2ラウンドが始まるやつ！」
四国めたん「共感した方は“React型談義”ってコメントで教えてください！」
ずんだもん「あなたのTypeScriptあるあるも募集中だよ！」

## 画面表示用コード

```typescript
import { useState } from "react";

const [value, setValue] = useState<string | null>(null);
setValue("ready");
setValue(null);

type Props = {
  status: "idle" | "loading" | "done";
};

const Badge = ({ status }: Props) => <span>{status}</span>;

type ClickHandler = React.MouseEventHandler<HTMLButtonElement>;

const handleClick: ClickHandler = event => {
  event.preventDefault();
};
```

## 制作メモ
- **想定シーン**: `useState<string | null>`って本当に必要？に遭遇した瞬間を切り取るネタ
- **狙いトーン**: 開発会話っぽく軽快に
- **技術キーワード**: React, useState, JSX
- **使いたい一言**: 「React型談義」
- **CTA案**: コメントで「React型談義」と書いてもらい、同じ経験を募る
- **備考**: React Hook周りの型ギャップを笑いに変えましょう。
