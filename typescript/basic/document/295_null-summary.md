# #295 「null型まとめ」

四国めたん「null型のまとめを学びましょう!」
ずんだもん「ここまで学んだことを整理するんだね!」
四国めたん「はい。まずUnion型でnullを明示的に扱います。」
ずんだもん「!=== nullでチェックしてから使うんだよね!」
四国めたん「その通りです。Nullish Coalescing??でデフォルト値も設定できます。」
ずんだもん「オプショナルチェーン?.と組み合わせると便利!」
四国めたん「はい。実践的なRepositoryパターンでもよく使われます。」
ずんだもん「型安全にnullを扱って、バグを防ぐのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: null型の基本 */
let value: string | null = null;
if (value !== null) {
  value.toUpperCase(); // string型
}

/** Example 2: Nullish Coalescing */
const name = userName ?? "Guest";
const config = settings?.timeout ?? 5000;

/** Example 3: 実践パターン */
function findUser(id: number): User | null {
  return users.find(u => u.id === id) ?? null;
}
```
