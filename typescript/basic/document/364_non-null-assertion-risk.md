# #364 「!演算子の危険性」

四国めたん「!を乱用すると実行時エラーを招きます。」
ずんだもん「document.getElementById("app")! でnullだったらクラッシュだね…」
四国めたん「はい。存在しないDOMを信じてinnerHTMLを触ると落ちます。」
ずんだもん「配列検索でも users.find(...)! って危ない?」
四国めたん「該当要素が無ければundefinedに対してプロパティを読んでしまいます。」
ずんだもん「安全に書くならチェックを入れるべき?」
四国めたん「if (element !== null) { ... } のようにガードすれば例外を防げます。」
ずんだもん「!の裏に潜むリスクを常に意識するのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: 危険なDOMアクセス */
const element = document.getElementById("app")!;
element.innerHTML = "Hello";  // elementがnullならエラー

/** Example 2: 実行時エラーになる例 */
const user = users.find(u => u.id === 999)!;
console.log(user.name);  // undefinedならエラー

/** Example 3: 安全な代替 */
const element = document.getElementById("app");
if (element !== null) {
  element.innerHTML = "Hello";  // 安全
}
```
