# #373 「void型の宣言」

四国めたん「void型を付ける方法を3パターンで押さえましょう。」
ずんだもん「まずは普通の関数宣言で: void を書くんだね。」
四国めたん「はい。function greet(): void のように型注釈します。」
ずんだもん「アロー関数も (msg: string): void => ... と書ける?」
四国めたん「もちろんです。代入するだけで同じ意味になります。」
ずんだもん「関数型を定義して共有するやり方もあるの?」
四国めたん「type Callback = (data: string) => void のように別名化すれば再利用できます。」
ずんだもん「宣言パターンを覚えてvoidを使い分けるのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: 関数宣言 */
function greet(name: string): void {
  console.log(`Hello, ${name}`);
}

/** Example 2: アロー関数 */
const log = (msg: string): void => {
  console.log(msg);
};

/** Example 3: 関数型の定義 */
type VoidFunction = () => void;
type Callback = (data: string) => void;
const handler: Callback = (data) => console.log(data);
```
