# #394 「forEach()の戻り値」

四国めたん「forEachはコールバックも戻り値もvoidです。」
ずんだもん「result = items.forEach(...) とするとresultはundefinedなんだね。」
四国めたん「そうです。型定義でもreturn voidとなっています。」
ずんだもん「コールバックの返り値は完全に無視される?」
四国めたん「はい。console.logだけする例が典型です。」
ずんだもん「mapとの違いは戻り値の有無なんだね。」
四国めたん「mapは新しい配列を返しますが、forEachは副作用専用です。」
ずんだもん「用途に合わせてメソッドを選ぶのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: forEach()の型定義 */
const items = [1, 2, 3];
const result = items.forEach(item => console.log(item));
console.log(result);

/** Example 2: コールバックの戻り値は無視 */
items.forEach((item): void => {
  console.log(item * 2);
});

/** Example 3: mapとの違い */
const doubled = items.map(x => x * 2);
items.forEach(x => console.log(x * 2));
```
