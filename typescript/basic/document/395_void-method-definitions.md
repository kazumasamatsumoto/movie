# #395 「メソッド定義」

四国めたん「オブジェクトのメソッドにもvoidを付けて意図を示せます。」
ずんだもん「logger.logのようにメソッド記法で書くと読みやすいね。」
四国めたん「errorも同じく副作用だけです。」
ずんだもん「アロー関数でプロパティに代入するスタイルでもvoidが使える?」
四国めたん「はい。utils.logのように書けます。」
ずんだもん「型定義でlogとwarnをまとめて宣言すると再利用しやすい!」
四国めたん「メソッドの役割を明示してチームで共有しましょう。」
ずんだもん「voidメソッドで副作用を分離するのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: オブジェクトリテラルのメソッド */
const logger = {
  log(message: string): void {
    console.log(`[LOG] ${message}`);
  },
  error(message: string): void {
    console.error(`[ERROR] ${message}`);
  }
};

/** Example 2: メソッド記法 */
const utils = {
  log: (msg: string): void => {
    console.log(msg);
  }
};

/** Example 3: 型定義 */
type Logger = {
  log(message: string): void;
  warn(message: string): void;
};
```
