# #438 「エッジケース」

四国めたん「voidにはエッジケースが存在します。」
ずんだもん「Callback = () => void なのにreturn 42; と書いても許されるって驚いた!」
四国めたん「コールバックの戻り値は無視されるためです。」
ずんだもん「function log(): void ではreturn 42; がエラーになるの?」
四国めたん「はい。宣言的なvoidは厳密です。」
ずんだもん「forEachのコールバックでもreturn x * 2; と書けるけど値は捨てられるんだね。」
四国めたん「エッジケースを理解して意図しないコードを書かないようにしましょう。」
ずんだもん「voidの挙動を正しく把握するのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: コールバックのvoid */

type Callback = () => void;
const cb: Callback = () => {
  return 42;
};

/** Example 2: 関数宣言では厳密 */

function log(): void {
  // return 42;
}

/** Example 3: forEachの例 */

[1, 2, 3].forEach((x): void => {
  return x * 2;
});
```
