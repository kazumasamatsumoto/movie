# #255 「比較演算まとめ」

四国めたん「比較演算のまとめを学びましょう!」
ずんだもん「ここまで学んだことを整理するんだね!」
四国めたん「はい。まず厳密等価演算子===の使用を推奨します。」
ずんだもん「浮動小数点数の比較には注意が必要だったよね!」
四国めたん「その通りです。isClose関数を使って許容誤差を設けます。」
ずんだもん「nullとundefinedは明示的にチェックするんだね!」
四国めたん「範囲チェックには>=や<=を組み合わせて使います。」
ずんだもん「ベストプラクティスを守って、型安全なコードを書くのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: 推奨: ===の使用 */
if (value === 0) { }

/** Example 2: 浮動小数点の比較 */
function isClose(a: number, b: number): boolean {
  return Math.abs(a - b) < 1e-10;
}

/** Example 3: null/undefinedチェックと範囲チェック */
if (value === null) { }
if (value === undefined) { }

// 範囲チェック
if (score >= 60 && score <= 100) { }
```
