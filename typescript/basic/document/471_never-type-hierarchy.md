# #471 「型階層」

四国めたん「neverは型階層の一番下に位置します。」
ずんだもん「fail()の戻り値をstringにもnumberにも代入できたね。」
四国めたん「逆方向、つまりstringからneverへの代入はできません。」
ずんだもん「ユニオン型ではstring | never がstringだけになるの?」
四国めたん「はい。neverは吸収されて消えます。」
ずんだもん「仕組みを理解すると型の挙動が読めるね。」
四国めたん「『何も存在しない』を示す型として覚えておきましょう。」
ずんだもん「neverの位置付けを意識するのだ!"

---

## 📺 画面表示用コード

```typescript
/** Example 1: neverはすべてに代入可能 */
function fail(): never {
  throw new Error("Failed");
}
const str: string = fail();
const num: number = fail();
const bool: boolean = fail();

/** Example 2: 逆方向は不可 */
let value: never;
// value = "string";

/** Example 3: ユニオン型でのnever */
type Result = string | never;
type Empty = never | never;
```
