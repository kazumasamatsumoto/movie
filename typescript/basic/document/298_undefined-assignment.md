# #298 「undefinedの代入」

四国めたん「undefinedの代入について学びましょう!」
ずんだもん「どんな値を代入できるの?」
四国めたん「はい。Union型で宣言した変数にundefinedや通常の値を代入できます。」
ずんだもん「string | undefinedならどちらも入るんだね!」
四国めたん「その通りです。柔軟に値を扱えます。」
ずんだもん「strictNullChecksでエラーになる場合は?」
四国めたん「number型にundefinedは代入できないので、Union型が必要です。」
ずんだもん「オプショナルプロパティは省略もできるのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: undefinedの代入 */
let name: string | undefined = undefined;
name = "Alice";  // OK
name = undefined; // OK

/** Example 2: strictNullChecks有効時 */
// let id: number = undefined;  // エラー
let id: number | undefined = undefined;  // OK

/** Example 3: オプショナルプロパティ */
const user: User = {
  name: "Alice",
  age: undefined  // オプショナルなので省略可能
};
```
