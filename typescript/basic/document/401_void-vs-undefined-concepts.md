# #401 「概念的違い」

四国めたん「voidとundefinedの概念的な違いを押さえましょう。」
ずんだもん「voidは戻り値を気にしない型なんだよね。」
四国めたん「はい。logMessageのように結果を使わない関数に使います。」
ずんだもん「undefinedは値としてundefinedを返す可能性を表すんだ?」
四国めたん「その通り。findItemは見つからないとundefinedを返します。」
ずんだもん「意図も違ってて、Loggerは副作用、Finderは検索結果って感じだね。」
四国めたん「用途に合わせて型を分けることが大切です。」
ずんだもん「概念を理解してvoidとundefinedを使い分けるのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: void: 戻り値を無視 */
function logMessage(msg: string): void {
  console.log(msg);
}

/** Example 2: undefined: undefined値を返す */
function findItem(id: number): Item | undefined {
  return items.find(item => item.id === id);
}

/** Example 3: 意図の違い */
type Logger = () => void;
type Finder = () => Item | undefined;
```
