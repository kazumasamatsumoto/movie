# #455 「デバッグ(2)」

四国めたん「void関数では状態を記録する道具も活用しましょう。」
ずんだもん「timerで前後にログを入れて実行時間を計測してた!」
四国めたん「fail-safeとしてtry-catchでログを残す例もあります。」
ずんだもん「戻り値ではなくログやメトリクスで追いかけるんだね。」
四国めたん「副作用関数の観測手段を用意しておくと安心です。」

---

## 📺 画面表示用コード

```typescript
/** Example 1: 時間計測 */
function timer(task: () => void): void {
  console.log('start');
  task();
  console.log('end');
}

/** Example 2: try-catchログ */
function safeProcess(): void {
  try {
    risky();
  } catch (error) {
    console.error('Failed:', error);
  }
}

/** Example 3: メトリクス送信 */
function process(): void {
  metrics.increment('process.start');
  work();
  metrics.increment('process.end');
}
```
