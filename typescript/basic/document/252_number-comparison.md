# #252 「数値の比較」

四国めたん「数値の比較について学びましょう!」
ずんだもん「整数の比較は簡単だよね!」
四国めたん「はい。ただし浮動小数点数には注意が必要です。」
ずんだもん「0.1 + 0.2 === 0.3がfalseになるって聞いたことある!」
四国めたん「その通りです。浮動小数点の精度の問題で誤差が生じます。」
ずんだもん「どうやって解決するの?」
四国めたん「許容誤差（epsilon）を使った比較関数を作ります。」
ずんだもん「Math.absで差を計算して、微小な誤差を許容するんだね!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: 整数の比較 */
console.log(10 > 5);    // true
console.log(10 === 10); // true

/** Example 2: 浮動小数点の問題 */
console.log(0.1 + 0.2 === 0.3); // false

/** Example 3: 許容誤差を使った比較 */
function isClose(a: number, b: number, epsilon = 1e-10): boolean {
  return Math.abs(a - b) < epsilon;
}

console.log(isClose(0.1 + 0.2, 0.3)); // true
```
