# #322 "Third-Party Libraries"

Shikoku Metan: "Let's learn about strictNullChecks support for third-party libraries!"
Zundamon: "We create type definition files!"
Shikoku Metan: "That's right. Create type definitions in types/legacy-lib.d.ts."
Zundamon: "We can also use skipLibCheck, right?"
Shikoku Metan: "Exactly. Use skipLibCheck as a temporary measure."
Zundamon: "There's also a method to handle it with type assertions!"
Shikoku Metan: "Yes. Use as to explicitly specify the string | null type."
Zundamon: "We properly manage the type definitions of libraries!"

---

## 📺 Code for Display

```typescript
/** Example 1: Create type definition file */
// types/legacy-lib.d.ts
declare module 'legacy-lib' {
  export function getData(): string | null;
}

/** Example 2: skipLibCheck (temporary) */
{
  "compilerOptions": {
    "strictNullChecks": true,
    "skipLibCheck": true
  }
}

/** Example 3: Handle with type assertion */
import { getData } from 'legacy-lib';
const data = getData() as string | null;
```
