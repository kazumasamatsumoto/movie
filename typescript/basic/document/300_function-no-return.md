# #300 「関数の戻り値なし」

四国めたん「関数の戻り値なしについて学びましょう!」
ずんだもん「void型とundefined型の違いは?」
四国めたん「はい。void型は戻り値を使わない、undefined型は明示的にundefinedを返します。」
ずんだもん「void関数は暗黙的にundefinedを返すんだね!」
四国めたん「その通りです。return文がない場合、自動的にundefinedが返されます。」
ずんだもん「find関数とかはUser | undefinedを返すの?」
四国めたん「はい。見つからない場合はundefinedを返す設計がよく使われます。」
ずんだもん「戻り値の型を明示して、意図を明確にするのが大事なのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: void型: 戻り値を使わない */
function log(message: string): void {
  console.log(message);
  // return undefined; が暗黙的
}

/** Example 2: undefined型: 明示的にundefinedを返す */
function find(): User | undefined {
  return undefined;
}

/** Example 3: 暗黙的なundefined */
function noReturn() {
  // 何もreturnしない
}
const result = noReturn(); // undefined
```
