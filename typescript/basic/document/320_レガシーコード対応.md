# #320 「レガシーコード対応」

四国めたん「レガシーコードへのstrictNullChecks対応について学びましょう!」
ずんだもん「Non-Null Assertion!は一時的な対応として使えるんだね!」
四国めたん「はい。null/undefinedでないと保証できる場合に使用します。」
ずんだもん「型アサーションasも一時的な対応として便利だよね?」
四国めたん「その通りです。ただし、TODOコメントを付けて後で修正しましょう。」
ずんだもん「any型の関数も段階的に修正していくんだね!」
四国めたん「はい。レガシー部分にTODOを残し、新規コードは厳格に書きます。」
ずんだもん「一時的な回避策を使いつつ、段階的に改善していくのだ!」

---


```typescript
/** Example 1: Non-Null Assertion (一時的) */
const user = getUser()!; // null/undefinedでないと保証
user.name; // OK

/** Example 2: 型アサーション (一時的) */
const data = response as User;
// TODO: 適切なnullチェックに修正

/** Example 3: 段階的修正 */
function legacyFunc(value: any) {
  // TODO: strictNullChecks対応
  return value.toString();
}
```
