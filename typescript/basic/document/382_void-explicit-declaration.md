# #382 「明示的宣言」

四国めたん「void型を明示すべき場面を押さえましょう。」
ずんだもん「initializeみたいな公開関数では: void を書いた方が親切なんだね。」
四国めたん「はい。インターフェースのLogger.logも同じく型を明示します。」
ずんだもん「逆に、内部のlogErrorみたいな関数は推論に任せてもいいの?」
四国めたん「チームのガイドライン次第ですが、小さな内部関数では省略されがちです。」
ずんだもん「ESLintでexplicit-function-return-typeを有効にすると強制できるんだ!」
四国めたん「ええ。必要ならルールで統一してミスを防げます。」
ずんだもん「明示と自動を使い分ける基準を持つのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: 明示すべき場面 */
export function initialize(config: Config): void {
  // 公開API
}
interface Logger {
  log(message: string): void;
}

/** Example 2: 推論でも良い場面 */
const logError = (err: Error) => {
  console.error(err);
};

/** Example 3: ESLintでの強制 */
// .eslintrc.json
{
  "rules": {
    "@typescript-eslint/explicit-function-return-type": "error"
  }
}
```
