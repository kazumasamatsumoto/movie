# #321 "Migration"

Shikoku Metan: "Let's learn the migration procedure to strictNullChecks!"
Zundamon: "First, we enable strictNullChecks!"
Shikoku Metan: "That's right. Set strictNullChecks to true in tsconfig.json."
Zundamon: "Next, we explicitly specify null allowance in type definitions, right?"
Shikoku Metan: "Exactly. Specify it explicitly like string | null."
Zundamon: "Finally, we implement null checks!"
Shikoku Metan: "Yes. Make it safe using type guards and optional chaining."
Zundamon: "We migrate gradually to create type-safe code!"

---

## 📺 Code for Display

```typescript
/** Example 1: Enable strictNullChecks */
{
  "compilerOptions": {
    "strictNullChecks": true
  }
}

/** Example 2: Add type definitions */
interface User {
  name: string;
  email: string | null; // null許容を明示
}

/** Example 3: Implement null checks */
function getUser(id: number): User | null {
  const user = findUser(id);
  if (!user) return null;
  return user;
}
```
