# #334 「配列がnullable」

四国めたん「配列自体がnullableな場合について学びましょう!」
ずんだもん「配列全体がnullになる可能性があるんだね!」
四国めたん「はい。string[] | null で、配列自体がnullまたは文字列配列になります。」
ずんだもん「操作する前にnullチェックが必要なの?」
四国めたん「その通りです。if (items !== null) でnullチェックしてから、forEachなどの操作を行います。」
ずんだもん「Optional Chainingも使えるんだね!」
四国めたん「はい。items?.length ?? 0 や items?.[0] のように、安全にアクセスできます。」
ずんだもん「配列のnullableを理解して、適切に処理するのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: nullable配列の宣言 */
let items: string[] | null = null;
items = ["a", "b", "c"]; // OK

/** Example 2: nullチェック後の操作 */
if (items !== null) {
  items.forEach(item => console.log(item));
}

/** Example 3: Optional Chainingで安全にアクセス */
const length = items?.length ?? 0;
const first = items?.[0];
```
