# #335 「nullableオブジェクト」

四国めたん「nullableオブジェクトについて学びましょう!」
ずんだもん「関数がnullを返す可能性がある場合なんだね!」
四国めたん「はい。User | null を返す関数で、オブジェクトが見つからない場合にnullを返します。」
ずんだもん「プロパティにアクセスする時はどうするの?」
四国めたん「その通りです。user?.name や user?.age ?? 0 のように、Optional Chainingを使います。」
ずんだもん「型ガードで安全に処理できるんだね!」
四国めたん「はい。if (user !== null) でnullチェックすると、ブロック内でUser型として扱えます。」
ずんだもん「nullableオブジェクトを適切に扱って、安全なコードを書くのだ!」

---

## 📺 画面表示用コード

```typescript
/** Example 1: nullableオブジェクトを返す関数 */
function findUser(id: number): User | null {
  return users.find(u => u.id === id) ?? null;
}

/** Example 2: Optional Chainingでアクセス */
const user = findUser(1);
const name = user?.name;
const age = user?.age ?? 0;

/** Example 3: 型ガードで安全に */
if (user !== null) {
  console.log(user.name); // User型
  console.log(user.age);
}
```
