四国めたん「Let's learn about `return undefined` in `void` functions!」
ずんだもん「Can a `void` function return `undefined`?」
四国めたん「Yes—`return undefined` is allowed because it's compatible with `void`.」
ずんだもん「Why is that permitted?」
四国めたん「`void` functions already yield `undefined`, so stating it explicitly doesn't conflict, though it's unnecessary.」
ずんだもん「So it's better not to write it?」
四国めたん「Right; prefer `return;` or omit the return, even in TypeScript v5.9.」
ずんだもん「Keeping it simple is best!」

