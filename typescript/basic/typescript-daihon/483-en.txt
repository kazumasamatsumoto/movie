四国めたん「Let's relate `try`-`catch` to `never`.」
ずんだもん「Can I catch a `never` function's error?」
四国めたん「Sure—wrap it in `try`-`catch`; the caller handles the exception.」
ずんだもん「Then what does `never` mean?」
四国めたん「The function itself never returns, but the exception propagates to be caught elsewhere.」
ずんだもん「So the control flow just changes lanes!」
四国めたん「That’s the standard error-handling pattern—even in TypeScript v5.9.」
ずんだもん「It keeps error handling safe!」

