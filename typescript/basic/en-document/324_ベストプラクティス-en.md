# #324 "Best Practices"

Shikoku Metan: "Let's learn best practices for strictNullChecks!"
Zundamon: "Explicit type definitions are important!"
Shikoku Metan: "That's right. Explicitly specify the return type as User | null."
Zundamon: "Using type guards is also important, right?"
Shikoku Metan: "Exactly. Access properties safely with null checks."
Zundamon: "Nullish Coalescing is also convenient!"
Shikoku Metan: "Yes. You can concisely specify default values with the ?? operator."
Zundamon: "We write safe code following best practices!"

---

## 📺 Code for Display

```typescript
/** Example 1: Explicit type definition */
function getUser(id: number): User | null {
  return users.find(u => u.id === id) ?? null;
}

/** Example 2: Type guard */
if (user !== null) {
  console.log(user.name); // 安全
}

/** Example 3: Nullish Coalescing */
const name = user?.name ?? "Guest";
const port = config.port ?? 3000;
```
