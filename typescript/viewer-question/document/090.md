# #090 「ライブラリの型配布、`.d.ts`派？`types`派？」制作ドキュメント

## 台本
ずんだもん「ライブラリの型配布、`.d.ts`派？`types`派？」
四国めたん「最近のTypeScript v5.9でもライブラリの型配布、`.d.ts`派の判断はチームごとに割れていますよね」
ずんだもん「ぼくはライブラリの型配布、`.d.ts`派寄りだけど、`types`派派の気持ちもわかるんだよね」
四国めたん「ライブラリの型配布、`.d.ts`派にはメリットがあるし、`types`派にも別の強みがあります。状況によって最適解が変わります」
ずんだもん「現場だとライブラリの型配布、`.d.ts`派を巡って会議が長引いて、ガイドラインを書き直すイベントが発生するんだよ！」
四国めたん「みなさんはどうしていますか？コメントで“ライブラリの型配布、`.d.ts`派の意見”って教えてください！」
ずんだもん「理由も添えてくれたら嬉しいな！」

## 画面表示用コード

```typescript
type Choice<A, B> = { current: A; alternative: B };

const poll = <T extends string>(label: T) => ({ label });

function vote<T>(value: T) {
  return value;
}
```

## 制作メモ
- **問いの切り口**: ライブラリの型配布、`.d.ts`派？`types`派？
- **想定視聴者層**: TypeScriptの設計やルール決めで悩むエンジニア
- **押さえる技術キーワード**: ライブラリの型配布、`.d.ts`派
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場の迷いを代弁
- **コメントで聞きたいこと**: ライブラリの型配布、`.d.ts`派の意見
- **補足メモ**: テーマに対する賛否や運用方針をコメントで募る汎用テンプレート。
- **進行トーン**: 議論を歓迎する落ち着いたテンポ
