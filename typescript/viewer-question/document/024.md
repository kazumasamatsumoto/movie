# #024 「`esbuild`で型チェック省略、アリ？ナシ？」制作ドキュメント

## 台本
ずんだもん「`esbuild`で型チェック省略、アリ？ナシ？」
四国めたん「esbuildで型チェックを省略する運用は`esbuild`で型チェック省略、アリでも賛否が分かれますね」
ずんだもん「ぼくは開発中は`esbuild`で型チェック省略、アリ派だけど、最終的にはナシを走らせたくなるんだよね」
四国めたん「`esbuild`で型チェック省略、アリは速いけれど型チェックが抜け、ナシは安心だけど遅いというジレンマです」
ずんだもん「現場だと『チェックはCIで』と割り切ったら、ローカルで見逃した型バグが本番寸前まで行く事件があるんだよ！」
四国めたん「みなさんはどうしていますか？コメントで“esbuild運用”って教えてください！」
ずんだもん「理由も添えてくれたら嬉しいな！」

## 画面表示用コード

```typescript
// package.json
{
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/app.js",
    "typecheck": "tsc --noEmit"
  }
}

import { createServer } from "node:http";

createServer((_req, res) => {
  res.end("ok");
}).listen(3000);
```

## 制作メモ
- **問いの切り口**: `esbuild`で型チェック省略、アリ？ナシ？
- **想定視聴者層**: TypeScriptの設計やルール決めで悩むエンジニア
- **押さえる技術キーワード**: esbuild, `tsc --noEmit`
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場の迷いを代弁
- **コメントで聞きたいこと**: esbuild運用
- **補足メモ**: 型検査をどこで走らせるかの運用をコメントで共有。
- **進行トーン**: スピード vs 安全の議論を促す
