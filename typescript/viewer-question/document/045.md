# #045 「`experimentalDecorators`まだ使ってる？」制作ドキュメント

## 台本
ずんだもん「`experimentalDecorators`まだ使ってる？」
四国めたん「デコレータはStage 3到達で`experimentalDecorators`まだ使ってるの判断が改めて問われていますね」
ずんだもん「ぼくは`experimentalDecorators`まだ使ってるはこうしてる派だけど、別のやり方も全然アリだと思ってるんだよね」
四国めたん「`experimentalDecorators`まだ使ってるにも利点とコストがあって、現場事情で判断が分かれますよね」
ずんだもん「現場だと切り替えた瞬間にビルドが怒って、metadata周りを総見直しすることがあるんだよ！」
四国めたん「みなさんはどうしていますか？コメントで“decorator運用”って教えてください！」
ずんだもん「理由も添えてくれたら嬉しいな！」

## 画面表示用コード

```typescript
function Log(prefix: string) {
  return function (
    _target: unknown,
    _propertyKey: string | symbol,
    descriptor: PropertyDescriptor,
  ) {
    const fn = descriptor.value as Function;
    descriptor.value = function (...args: unknown[]) {
      console.log(prefix, args);
      return fn.apply(this, args);
    };
  };
}

class Service {
  @Log("call")
  run() {
    return "ok";
  }
}

new Service().run();
```

## 制作メモ
- **問いの切り口**: `experimentalDecorators`まだ使ってる？
- **想定視聴者層**: TypeScriptの設計やルール決めで悩むエンジニア
- **押さえる技術キーワード**: デコレータ, Stage 3, `experimentalDecorators`
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場の迷いを代弁
- **コメントで聞きたいこと**: decorator運用
- **補足メモ**: 正式採用/従来構文の使い分けをコメントで共有。
- **進行トーン**: 慎重派と歓迎派の温度差を演出
