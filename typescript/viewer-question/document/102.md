# #102 「`template literal types`の活用シーン、教えて！」制作ドキュメント

## 台本
ずんだもん「`template literal types`の活用シーン、教えて！」
四国めたん「v5.9では`template literal types`のパフォーマンスが改善されて、より複雑なパターンも扱いやすくなりました」
ずんだもん「ぼくはイベント名の型安全化やルーティングパスの型チェックに使うのが好きなんだよね」
四国めたん「文字列リテラル型を組み合わせれば、動的な文字列パターンを型レベルで表現できます」
ずんだもん「現場だとAPIエンドポイントの型定義に使うと、typoによるバグが激減するんだよ！」
四国めたん「みなさんはどんな場面で活用していますか？」
ずんだもん「実用的な使い方をコメントで共有してね！」

## 画面表示用コード

```typescript
type HttpMethod = "GET" | "POST" | "PUT";
type Endpoint = "users" | "posts" | "comments";
type ApiRoute = `/${Endpoint}`;

type EventName<T extends string> = `on${Capitalize<T>}`;
type ClickEvent = EventName<"click">; // "onClick"
```

## 制作メモ
- **問いの切り口**: template literal typesをどこで活用しているか
- **想定視聴者層**: TypeScriptの文字列型を活用したいエンジニア
- **押さえる技術キーワード**: template literal types, 文字列リテラル型
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場視点
- **コメントで聞きたいこと**: 実用的な活用シーン
- **補足メモ**: APIルーティングやイベント名の型安全化の例を紹介
