# #122 「`Result<T, E>`型、導入したいと思ってる？」制作ドキュメント

## 台本
ずんだもん「`Result<T, E>`型、導入したいと思ってる？」
四国めたん「v5.9では判別可能なユニオン型がさらに強化され、`Result`型パターンの実装が快適になっています」
ずんだもん「ぼくは例外を使わずにエラーを値として扱う`Result`型が、関数型っぽくて好きなんだよね」
四国めたん「`Result`型を使うと、エラーハンドリングが型システムで強制され、エラーの見落としを防げます」
ずんだもん「非同期処理の連鎖で`Result`型を使ったら、どこでエラーが起きたか追跡しやすくなったんだよ！」
四国めたん「みなさんは`Result`型パターンを導入していますか？」
ずんだもん「使ってる感想や実装例をコメントで教えてね！」

## 画面表示用コード

```typescript
type Result<T, E> =
  | { success: true; value: T }
  | { success: false; error: E };

function divide(a: number, b: number): Result<number, string> {
  if (b === 0) {
    return { success: false, error: 'Division by zero' };
  }
  return { success: true, value: a / b };
}

const result = divide(10, 2);
if (result.success) {
  console.log('Result:', result.value);
} else {
  console.error('Error:', result.error);
}
```

```typescript
// チェーン処理
function processData(input: string): Result<number, string> {
  const parsed = parseInt(input);
  if (isNaN(parsed)) {
    return { success: false, error: 'Invalid number' };
  }

  const divided = divide(100, parsed);
  if (!divided.success) {
    return divided;
  }

  return { success: true, value: divided.value * 2 };
}
```

## 制作メモ
- **問いの切り口**: Result型パターンの導入意向
- **想定視聴者層**: 関数型プログラミングに興味があるエンジニア
- **押さえる技術キーワード**: Result型, 判別可能なユニオン, エラーハンドリング
- **キャラの立場**: 四国めたん=型システムの利点解説 / ずんだもん=関数型アプローチ推奨
- **コメントで聞きたいこと**: Result型の導入状況や感想
- **補足メモ**: 例外を使わない明示的なエラーハンドリングの利点を強調
