# #002 「`satisfies`って本番で使える？使えない？」制作ドキュメント

## 台本
ずんだもん「`satisfies`って本番で使える？使えない？」
四国めたん「`satisfies`はリテラル推論を崩さず構造チェックできるので`satisfies`って本番で使えるの議論が熱いですよね」
ずんだもん「ぼくは`satisfies`って本番で使えるで静的保証を固めたい派だけど、緊急対応では使えないの柔軟さに甘えたくなるんだよね」
四国めたん「`satisfies`って本番で使えるは余計なプロパティを弾ける安心感、使えないは記述量が少なくスピード重視なところが魅力です」
ずんだもん「現場だと`satisfies`を入れた瞬間に警告が噴出して、棚卸し会議が始まるんだよ！」
四国めたん「みなさんはどうしていますか？コメントで“satisfies運用”って教えてください！」
ずんだもん「理由も添えてくれたら嬉しいな！」

## 画面表示用コード

```typescript
const config = {
  mode: "strict",
  retries: 2,
} satisfies {
  mode: "strict" | "relaxed";
  retries: number;
};

const loose = {
  mode: "strict",
  retries: 2,
} as {
  mode: "strict" | "relaxed";
  retries: number;
};

type Mode = typeof config.mode;
```

## 制作メモ
- **問いの切り口**: `satisfies`って本番で使える？使えない？
- **想定視聴者層**: TypeScriptの設計やルール決めで悩むエンジニア
- **押さえる技術キーワード**: `satisfies`, リテラル保持, 型検証
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場の迷いを代弁
- **コメントで聞きたいこと**: satisfies運用
- **補足メモ**: `satisfies`導入の可否や運用ルールをコメントで募る。
- **進行トーン**: 慎重派と攻め派を仲裁するテンポ
