# #040 「`tsconfig`の`lib`設定、デフォルト？調整派？」制作ドキュメント

## 台本
ずんだもん「`tsconfig`の`lib`設定、デフォルト？調整派？」
四国めたん「最近のTypeScript v5.9でも`tsconfig`の`lib`設定、デフォルトの判断はチームごとに割れていますよね」
ずんだもん「ぼくは`tsconfig`の`lib`設定、デフォルト寄りだけど、調整派派の気持ちもわかるんだよね」
四国めたん「`tsconfig`の`lib`設定、デフォルトにはメリットがあるし、調整派にも別の強みがあります。状況によって最適解が変わります」
ずんだもん「現場だと`tsconfig`の`lib`設定、デフォルトを巡って会議が長引いて、ガイドラインを書き直すイベントが発生するんだよ！」
四国めたん「みなさんはどうしていますか？コメントで“`tsconfig`の`lib`設定、デフォルトの意見”って教えてください！」
ずんだもん「理由も添えてくれたら嬉しいな！」

## 画面表示用コード

```typescript
type Choice<A, B> = { current: A; alternative: B };

const poll = <T extends string>(label: T) => ({ label });

function vote<T>(value: T) {
  return value;
}
```

## 制作メモ
- **問いの切り口**: `tsconfig`の`lib`設定、デフォルト？調整派？
- **想定視聴者層**: TypeScriptの設計やルール決めで悩むエンジニア
- **押さえる技術キーワード**: `tsconfig`の`lib`設定、デフォルト
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場の迷いを代弁
- **コメントで聞きたいこと**: `tsconfig`の`lib`設定、デフォルトの意見
- **補足メモ**: テーマに対する賛否や運用方針をコメントで募る汎用テンプレート。
- **進行トーン**: 議論を歓迎する落ち着いたテンポ
