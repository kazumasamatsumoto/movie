# #018 「`isolatedModules`有効にしてる人いる？」制作ドキュメント

## 台本
ずんだもん「`isolatedModules`有効にしてる人いる？」
四国めたん「`isolatedModules`はESM互換を意識するなら外せない設定で、`isolatedModules`有効にしてる人いるの判断が悩ましいですね」
ずんだもん「ぼくはビルド互換のためON派だけど、テスト環境ではOFFにしたくなるんだよね」
四国めたん「ONにするとファイル単位で安全になりますが、`export type`漏れとの戦いが始まります」
ずんだもん「現場だとONに切り替えた瞬間に型の再エクスポートが怒られて、深夜に修正祭りが開かれるんだよ！」
四国めたん「みなさんはどうしていますか？コメントで“isolatedModules運用”って教えてください！」
ずんだもん「理由も添えてくれたら嬉しいな！」

## 画面表示用コード

```typescript
// tsconfig.json
{
  "compilerOptions": {
    "isolatedModules": true
  }
}

export type User = {
  id: string;
};

export const createUser = (id: string): User => ({ id });
```

## 制作メモ
- **問いの切り口**: `isolatedModules`有効にしてる人いる？
- **想定視聴者層**: TypeScriptの設計やルール決めで悩むエンジニア
- **押さえる技術キーワード**: `isolatedModules`, ESM互換
- **キャラの立場**: 四国めたん=整理役 / ずんだもん=実装現場の迷いを代弁
- **コメントで聞きたいこと**: isolatedModules運用
- **補足メモ**: ON/OFFの理由や運用での注意点をコメントで募る。
- **進行トーン**: 設定トラブルをネタにするテンポ
