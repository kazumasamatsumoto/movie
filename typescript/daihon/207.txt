# #207 「if文での使用」台本

四国めたん「TypeScript v5.9のif文でのboolean型使用について学びましょう！」
ずんだもん「if文でのboolean型使用って何？」
四国めたん「if文の条件式でboolean型の変数を使用する方法です」
ずんだもん「どうやって使うの？」
四国めたん「if (flag)のようにboolean型の変数を条件式に使用します」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、条件分岐の制御に重要です」
ずんだもん「if文で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// if文での使用
let isActive: boolean = true;
let isCompleted: boolean = false;

// 基本的な使用
if (isActive) {
  console.log("アクティブです");
}

if (!isCompleted) {
  console.log("未完了です");
}

// 実用的な例
let userLoggedIn: boolean = true;
if (userLoggedIn) {
  console.log("ユーザーがログインしています");
}
```
