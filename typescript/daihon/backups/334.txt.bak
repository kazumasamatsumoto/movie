# #334「配列がnullable」

四国めたん「配列全体がnullableな型について学びましょう！」
ずんだもん「配列自体がnullってどういうこと？」
四国めたん「T[] | null のように配列全体がnullの可能性がある型です。配列が存在しない場合を表現できますね」
ずんだもん「どう使い分けるの？」
四国めたん「データがない場合はnull、空の配列は [] を使います。意味が異なりますね」
ずんだもん「なるほど！nullと空配列は違うんだね！」
四国めたん「配列操作前にnullチェックが必須です。Optional Chainingも使えますね」
ずんだもん「型でnullを明示できて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 配列がnullable
let items: string[] | null = null;
items = ["a", "b", "c"]; // OK
```

```typescript
// nullチェック後の操作
if (items !== null) {
  items.forEach(item => console.log(item));
}
```

```typescript
// Optional Chainingで安全にアクセス
const length = items?.length ?? 0;
const first = items?.[0];
```
