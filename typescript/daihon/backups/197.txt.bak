# #197 「間違い(2) - NaNチェック」台本

四国めたん「TypeScript v5.9のNaNチェックの間違いについて学びましょう！」
ずんだもん「NaNチェックの間違いって何？」
四国めたん「NaNを===で比較する間違いです」
ずんだもん「どんな問題があるの？」
四国めたん「NaNは自分自身とも等しくない特殊な値です」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、正確な数値検証に重要です」
ずんだもん「NaNチェックで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 間違い(2) - NaNチェック
let invalidNumber: number = Number("abc");

// ❌ 間違い
console.log(invalidNumber === NaN); // false

// ✅ 正しい方法
console.log(Number.isNaN(invalidNumber)); // true

// 実用的な例
function validateNumber(value: unknown): boolean {
  if (typeof value === "number") {
    return !Number.isNaN(value);
  }
  return false;
}

let userInput: unknown = "abc";
let converted: number = Number(userInput);
console.log(validateNumber(converted)); // false
```
