# #296「undefined型とは」

四国めたん「TypeScriptのundefined型について学びましょう！」
ずんだもん「undefined型ってどんな型なの？」
四国めたん「変数が未初期化または値が未定義であることを表す型です。JavaScriptのundefinedに対応しています」
ずんだもん「nullとは違うの？」
四国めたん「はい、undefinedは未定義、nullは明示的な空値です。strictNullChecksで厳密に区別されます」
ずんだもん「なるほど！初期化されてない時に使われるんだね！」
四国めたん「オプショナルプロパティや関数の戻り値、存在しないプロパティのアクセスで発生しますね」
ずんだもん「型安全にundefinedを扱えて便利だよ！」

---

## 📺 画面表示用コード

```typescript
// undefined型の基本
let value: undefined = undefined;
let name: string | undefined;
```

```typescript
// オプショナルプロパティ
interface User {
  name?: string;  // string | undefined
  age?: number;   // number | undefined
}
```

```typescript
// strictNullChecks有効時
let id: number = undefined; // エラー
let id: number | undefined = undefined; // OK
```
