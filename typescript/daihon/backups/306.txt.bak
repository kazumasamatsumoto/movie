# #306「undefinedとvoidの違い」

四国めたん「undefinedとvoid型の違いについて学びましょう！」
ずんだもん「undefinedとvoidって何が違うの？」
四国めたん「undefinedは値、voidは戻り値を使わないことを示す型です。意味が異なりますね」
ずんだもん「使い分けはどうすればいいの？」
四国めたん「関数が値を返さない場合はvoid、明示的にundefinedを返す場合はundefined型です」
ずんだもん「なるほど！意図が違うんだね！」
四国めたん「void型の関数も実際にはundefinedを返しますが、型システム上は戻り値を無視します」
ずんだもん「型で意図を明確にできて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// void型: 戻り値を使わない
function log(msg: string): void {
  console.log(msg);
}
log("Hello"); // 戻り値は使えない
```

```typescript
// undefined型: 明示的にundefined
function find(): User | undefined {
  return undefined;
}
const user = find(); // undefined
```

```typescript
// voidはundefinedを返す
function noReturn(): void { }
const result = noReturn(); // undefined (型はvoid)
```
