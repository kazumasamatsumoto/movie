# #038 「よくある間違い(2)」台本

四国めたん「TypeScript v5.9のテンプレートリテラルよくある間違い続きについて学びましょう！」
ずんだもん「他にも間違いがあるの？」
四国めたん「はい、エスケープの問題、複雑な式、パフォーマンスの問題などがあります」
ずんだもん「どうやって防ぐの？」
四国めたん「シンプルな構造、適切なエスケープ、効率的な書き方を心がけます」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、品質の高いコードを書くために重要です」
ずんだもん「品質向上で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// よくある間違いの例（続き）

// 4. 複雑すぎる式（間違い）
// let complex = `Result: ${someFunction() + anotherFunction() * 2}`; // 読みにくい

// 正しい方法
let result1: number = someFunction();
let result2: number = anotherFunction() * 2;
let simple: string = `Result: ${result1 + result2}`;

// 5. エスケープの問題
let codeExample: string = `Code: \`const name = "test"\``; // 正しいエスケープ

// 6. パフォーマンスの問題
let items: string[] = ["a", "b", "c"];
// 非効率: ループ内でテンプレートリテラル
// 効率: 事前に準備
let itemList: string = items.map(item => `- ${item}`).join('\n');
```