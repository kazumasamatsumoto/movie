# #413「パフォーマンス」

四国めたん「voidとundefinedのパフォーマンスについて学びましょう！」
ずんだもん「voidとundefinedでパフォーマンスは違うの？」
四国めたん「いいえ、実行時は同じです。どちらも undefined を返すため、パフォーマンス差はありませんね」
ずんだもん「じゃあ何が違うの？」
四国めたん「型チェックだけの違いです。コンパイル後のJavaScriptは同一ですね」
ずんだもん「なるほど！型レベルの違いなんだね！」
四国めたん「型システムの利点は開発時の安全性です。TypeScript v5.9でも実行時コストはゼロですね」
ずんだもん「型安全性を無料で得られるんだね！」

---

## 📺 画面表示用コード

```typescript
// TypeScript
function voidFunc(): void {
  console.log("void");
}
function undefFunc(): undefined {
  return undefined;
}
```

```typescript
// JavaScript (トランスパイル後は同じ)
function voidFunc() {
  console.log("void");
}
function undefFunc() {
  return undefined;
}
```

```typescript
// 実行時の動作は同じ
console.log(voidFunc());    // undefined
console.log(undefFunc());   // undefined
// パフォーマンス差はなし
```
