# #402「voidは「値を気にしない」」

四国めたん「voidの「値を気にしない」という意味について学びましょう！」
ずんだもん「void型は何を表してるの？」
四国めたん「関数が戻り値を返さない、または戻り値を使うべきでないことを表します。値そのものに意味がありませんね」
ずんだもん「どういう場面で使うの？」
四国めたん「副作用のための関数、イベントハンドラ、コールバックなどです。処理が目的で値は重要でないですね」
ずんだもん「なるほど！処理だけが大事なんだね！」
四国めたん「戻り値を使おうとするとコンパイルエラーになります。TypeScript v5.9でも同じですね」
ずんだもん「意図を強制できるんだね！」

---

## 📺 画面表示用コード

```typescript
// void: 値を気にしない
function updateUI(): void {
  render();
  // 戻り値は使わない
}
```

```typescript
// 戻り値を使おうとするとエラー
function process(): void {
  console.log("Processing");
}
const result: string = process();  // エラー: Type 'void' is not assignable to type 'string'
```

```typescript
// コールバック: 戻り値を無視
type Callback = (data: string) => void;
const handler: Callback = (data) => {
  console.log(data);  // 処理のみ
};
```
