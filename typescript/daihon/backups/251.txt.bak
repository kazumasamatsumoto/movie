# #251 「文字列の比較」台本

四国めたん「TypeScript v5.9の文字列の比較について学びましょう！」
ずんだもん「文字列はどう比較されるの？」
四国めたん「辞書順（lexicographic order）で比較されます」
ずんだもん「具体的にはどういうこと？」
四国めたん「文字コードの順番で比較されます。'a' < 'b'はtrueです」
ずんだもん「大文字と小文字は？」
四国めたん「大文字の方が小さい値になります。'A' < 'a'はtrueです」
ずんだもん「文字コードの知識が必要だね！」

---

## 📺 画面表示用コード

```typescript
// 文字列の比較

// 辞書順の比較
console.log('apple' < 'banana'); // true
console.log('cat' > 'dog');      // false

// 大文字と小文字
console.log('A' < 'a');  // true
console.log('Z' < 'a');  // true

// 長さの異なる文字列
console.log('abc' < 'abcd'); // true

// 型安全な文字列比較
function compareStrings(a: string, b: string): number {
  return a === b ? 0 : a < b ? -1 : 1;
}
