# #190 「数値計算まとめ」台本

四国めたん「TypeScript v5.9の数値計算まとめについて学びましょう！」
ずんだもん「数値計算まとめって何？」
四国めたん「数値計算の要点と重要な機能のまとめです」
ずんだもん「どんな機能があるの？」
四国めたん「IEEE 754、精度問題、回避方法、BigIntなどがあります」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、数値計算の基盤となる重要な機能です」
ずんだもん「数値計算で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 数値計算まとめ

// 1. 精度問題の理解
let num1: number = 0.1 + 0.2;
console.log(num1 === 0.3); // false

// 2. 回避方法
function addDecimals(a: number, b: number): number {
  const factor = 100;
  return (Math.round(a * factor) + Math.round(b * factor)) / factor;
}

// 3. 適切な比較
function isEqual(a: number, b: number): boolean {
  return Math.abs(a - b) < 1e-10;
}

// 4. BigIntの活用
let largeId: bigint = 1234567890123456789n;
```
