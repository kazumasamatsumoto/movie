# #453「間違い(3) - return値」

四国めたん「void関数でのreturn値の間違いについて学びましょう！」
ずんだもん「どんな間違いがあるの？」
四国めたん「void関数で値を返そうとすることです。undefined以外はエラーになりますね」
ずんだもん「どうして間違えるの？」
四国めたん「関数の途中で値を返したくなることがあります。しかしvoid型では許可されませんね」
ずんだもん「なるほど！型が制限してるんだね！」
四国めたん「値を返す必要がある場合は、戻り値型を変更します。TypeScript v5.9でも型チェックで防げますね」
ずんだもん「型を正しく設計することが大事なんだね！」

---

## 📺 画面表示用コード

```typescript
// 間違い: void関数で値を返す
function process(data: string): void {
  if (!data) {
    return false;  // エラー: Type 'boolean' is not assignable to type 'void'
  }
  console.log(data);
}
```

```typescript
// 正しい: 戻り値型を変更
function process(data: string): boolean {
  if (!data) {
    return false;
  }
  console.log(data);
  return true;
}
```

```typescript
// 正しい: voidのまま早期リターン
function process(data: string): void {
  if (!data) return;  // OK
  console.log(data);
}
```
