# #432「デフォルト型パラメータ」

四国めたん「デフォルト型パラメータとvoid型について学びましょう！」
ずんだもん「デフォルトでvoidを指定できるの？」
四国めたん「はい、型パラメータのデフォルト値として void を設定できます。省略時に void になりますね」
ずんだもん「どう書くの？」
四国めたん「T = void の形式で書きます。戻り値を期待しない場合のデフォルトとして便利ですね」
ずんだもん「なるほど！省略できるんだね！」
四国めたん「コールバック関数の型定義でよく使われます。TypeScript v5.9でも実用的なパターンですね」
ずんだもん「柔軟な型定義ができるんだね！」

---

## 📺 画面表示用コード

```typescript
// デフォルト型パラメータ
type Callback<T = void> = (value: T) => void;
const voidCallback: Callback = (v) => {
  console.log("Callback called");
};
```

```typescript
// 型引数を指定
const numberCallback: Callback<number> = (n) => {
  console.log(n * 2);
};
```

```typescript
// 複数の型パラメータ
type Handler<TData = void, TResult = void> =
  (data: TData) => TResult;
const logger: Handler = () => {
  console.log("Log");
};
```
