# #085 「バリデーション実例」台本

四国めたん「TypeScript v5.9のバリデーション実例について学びましょう！」
ずんだもん「バリデーション実例って何？」
四国めたん「実際のAPIでstring型のバリデーションを行う例です」
ずんだもん「どんな検証ができるの？」
四国めたん「必須チェック、長さチェック、形式チェックなどがあります」
ずんだもん「Nest.jsの開発でも重要？」
四国めたん「はい、安全なAPIの構築に重要な機能です」
ずんだもん「バリデーションで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// バリデーション実例
import { IsString, IsNotEmpty, Length, IsEmail } from 'class-validator';

export class CreateUserDto {
  @IsString()
  @IsNotEmpty()
  @Length(1, 50)
  name: string;

  @IsString()
  @IsEmail()
  email: string;

  @IsString()
  @IsNotEmpty()
  @Length(3, 20)
  username: string;
}

export class UpdateProfileDto {
  @IsString()
  @IsOptional()
  @Length(0, 200)
  bio?: string;
}
```