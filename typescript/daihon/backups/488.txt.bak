# #488「エラーバウンダリ」

四国めたん「エラーバウンダリについて学びましょう！」
ずんだもん「エラーバウンダリって何？」
四国めたん「エラーを特定の境界で捕捉する仕組みです。アプリ全体がクラッシュするのを防ぎますね」
ずんだもん「どう実装するの？」
四国めたん「try-catchで囲み、エラーを適切に処理します。Reactではコンポーネントとして実装しますね」
ずんだもん「なるほど！部分的に止めるんだね！」
四国めたん「ユーザー体験を向上させます。TypeScript v5.9でも重要なパターンですね」
ずんだもん「堅牢なアプリが作れるんだね！」

---

## 📺 画面表示用コード

```typescript
// エラーバウンダリの基本
function safeExecute<T>(fn: () => T, fallback: T): T {
  try {
    return fn();
  } catch (error) {
    console.error("Error caught:", error);
    return fallback;
  }
}
```

```typescript
// 非同期エラーバウンダリ
async function safeAsync<T>(
  fn: () => Promise<T>,
  fallback: T
): Promise<T> {
  try {
    return await fn();
  } catch (error) {
    console.error("Async error:", error);
    return fallback;
  }
}
```

```typescript
// Reactエラーバウンダリ
class ErrorBoundary extends React.Component {
  componentDidCatch(error: Error): void {
    console.error("Component error:", error);
  }
  render() {
    return this.props.children;
  }
}
```
