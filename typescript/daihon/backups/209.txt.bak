# #209 「三項演算子」台本

四国めたん「TypeScript v5.9の三項演算子でのboolean型使用について学びましょう！」
ずんだもん「三項演算子でのboolean型使用って何？」
四国めたん「三項演算子の条件式でboolean型の変数を使用する方法です」
ずんだもん「どうやって使うの？」
四国めたん「flag ? value1 : value2のようにboolean型の変数を条件式に使用します」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、条件付きの値設定に重要です」
ずんだもん「三項演算子で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 三項演算子での使用
let isActive: boolean = true;
let isCompleted: boolean = false;

// 基本的な使用
let status: string = isActive ? "アクティブ" : "非アクティブ";
let message: string = isCompleted ? "完了" : "未完了";

// 実用的な例
let userLoggedIn: boolean = true;
let displayText: string = userLoggedIn ? "ログイン中" : "ログアウト中";

console.log(status);    // "アクティブ"
console.log(message);   // "未完了"
console.log(displayText); // "ログイン中"
```
