# #240 「論理演算まとめ」台本

四国めたん「TypeScript v5.9の論理演算まとめについて学びましょう！」
ずんだもん「論理演算まとめって何？」
四国めたん「論理演算子の要点と重要な機能のまとめです」
ずんだもん「どんな機能があるの？」
四国めたん「AND、OR、NOT、型推論、短絡評価、優先順位などがあります」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、論理演算の基盤となる重要な機能です」
ずんだもん「論理演算まとめで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 論理演算まとめ

// 1. AND演算子（&&）
let isActive: boolean = true;
let hasPermission: boolean = false;
let canProceed: boolean = isActive && hasPermission; // false

// 2. OR演算子（||）
let canAccess: boolean = isActive || hasPermission; // true

// 3. NOT演算子（!）
let isInactive: boolean = !isActive; // false

// 4. 二重否定（!!）
let userName: string = "John";
let hasName: boolean = !!userName; // true

// 5. 実用的な例
let userLoggedIn: boolean = true;
let formValid: boolean = false;
let canSubmit: boolean = userLoggedIn && formValid; // false
```
