# #183 「イプシロン比較」台本

四国めたん「TypeScript v5.9のイプシロン比較について学びましょう！」
ずんだもん「イプシロン比較って何？」
四国めたん「浮動小数点数の等価性を判定する方法です」
ずんだもん「どうやって実現するの？」
四国めたん「微小な誤差範囲内での比較を行います」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、正確な数値比較に重要です」
ずんだもん「イプシロン比較で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// イプシロン比較
function epsilonEqual(a: number, b: number, epsilon: number = 1e-10): boolean {
  return Math.abs(a - b) < epsilon;
}

// 実用的な例
let num1: number = 0.1 + 0.2;
let num2: number = 0.3;

console.log(num1 === num2); // false
console.log(epsilonEqual(num1, num2)); // true

// 実用的な例
let price1: number = 100.50;
let price2: number = 100.50;
console.log(epsilonEqual(price1, price2)); // true
```
