# #284「nullの使用例」

四国めたん「nullの実践的な使用例について学びましょう！」
ずんだもん「nullって実際どんな場面で使うの？」
四国めたん「データベースの検索結果、オプショナルな設定、APIのレスポンスなど、値がない状態を明示する場合に使います」
ずんだもん「具体的なコード例を教えて！」
四国めたん「find関数の戻り値や、キャッシュの初期値などで使います。nullチェックと組み合わせて安全に処理できます」
ずんだもん「なるほど！データがない状態を表現できるんだね！」
四国めたん「Angularのサービスやインジェクションでも活用されていますね」
ずんだもん「実用的で型安全なコードが書けて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// データ検索とキャッシュ管理
function findUser(id: number): User | null {
  return users.find(u => u.id === id) ?? null;
}
class CacheService {
  private cache: Map<string, any> | null = null;
}
```

```typescript
// APIレスポンス
interface ApiResponse<T> {
  data: T | null;
  error: string | null;
}
```

```typescript
// Angular DI
@Injectable()
class UserService {
  private currentUser: User | null = null;
  setUser(user: User | null): void {
    this.currentUser = user;
  }
}
```
