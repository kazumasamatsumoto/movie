# #300「関数の戻り値なし」

四国めたん「関数の戻り値がない場合について学びましょう！」
ずんだもん「関数がreturnしないとどうなるの？」
四国めたん「明示的にreturnしない関数はundefinedを返します。戻り値の型はvoidまたはundefinedです」
ずんだもん「voidとundefinedの違いは？」
四国めたん「voidは戻り値を使わないことを示し、undefinedは明示的にundefinedを返すことを示します」
ずんだもん「なるほど！使い分けが必要なんだね！」
四国めたん「void型の関数も実際にはundefinedを返しますが、型システム上は無視されますね」
ずんだもん「型で意図を明確にできて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// void型: 戻り値を使わない
function log(message: string): void {
  console.log(message);
  // return undefined; が暗黙的
}
```

```typescript
// undefined型: 明示的にundefinedを返す
function find(): User | undefined {
  return undefined;
}
```

```typescript
// 暗黙的なundefined
function noReturn() {
  // 何もreturnしない
}
const result = noReturn(); // undefined
```
