# #084 「@IsString()デコレータ」台本

四国めたん「TypeScript v5.9の@IsString()デコレータについて学びましょう！」
ずんだもん「@IsString()デコレータって何？」
四国めたん「プロパティがstring型であることを検証するデコレータです」
ずんだもん「どうやって使うの？」
四国めたん「プロパティの上に@IsString()を付けて検証を追加します」
ずんだもん「Nest.jsの開発でも重要？」
四国めたん「はい、APIのバリデーションに重要な機能です」
ずんだもん「デコレータで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// @IsString()デコレータ
import { IsString, IsOptional } from 'class-validator';

export class UserDto {
  @IsString()
  name: string;

  @IsString()
  email: string;

  @IsString()
  @IsOptional()
  description?: string;
}

export class ProductDto {
  @IsString()
  name: string;

  @IsString()
  category: string;

  @IsString()
  @IsOptional()
  tags?: string;
}
```