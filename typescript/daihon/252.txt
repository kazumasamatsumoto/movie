# #252 「数値の比較」台本

四国めたん「TypeScript v5.9の数値の比較について学びましょう！」
ずんだもん「数値の比較で注意点は？」
四国めたん「浮動小数点数の精度問題に注意が必要です」
ずんだもん「具体的にはどういうこと？」
四国めたん「0.1 + 0.2 === 0.3はfalseになります」
ずんだもん「え、なぜfalseなの？」
四国めたん「浮動小数点の誤差が原因です。許容誤差を使って比較します」
ずんだもん「精度を考慮した比較が大事だね！」

---

## 📺 画面表示用コード

```typescript
// 数値の比較

// 整数の比較
console.log(10 > 5);    // true
console.log(10 === 10); // true

// 浮動小数点の問題
console.log(0.1 + 0.2 === 0.3); // false

// 許容誤差を使った比較
function isClose(a: number, b: number, epsilon = 1e-10): boolean {
  return Math.abs(a - b) < epsilon;
}

console.log(isClose(0.1 + 0.2, 0.3)); // true
