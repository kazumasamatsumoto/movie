四国めたん「Let's learn about the `unknown` type in TypeScript 」
ずんだもん「People call it a type-safe `any`—when do we reach for it?」
四国めたん「`unknown` is the top type, so anything assigns to it, but unlike `any` you get an error if you touch properties outright.」
ずんだもん「So arithmetic or method calls are off the table?」
四国めたん「Exactly—guard it with `typeof`, `instanceof`, or the `in` operator before you use it.」
ずんだもん「Helpers like `Array.isArray` or custom guards let us narrow it step by step.」
四国めたん「If you really need an `as` assertion, pair it with a prior check as best practice.」
ずんだもん「Use `unknown` for API responses or `catch` errors so we keep everything type-safe!」

