四国めたん「Let's examine `Promise.all()` with `Promise<void>`.」
ずんだもん「What does `Promise.all()` return here?」
四国めたん「It yields `Promise<void[]>`, but every element in the array is `undefined`.」
ずんだもん「So I can't really use the array?」
四国めたん「You usually ignore it—you just care that everything finished.」
ずんだもん「I get it! It's a completion check!」
四国めたん「You can also consider `Promise.allSettled()` or `Promise.race()`; they’re handy in TypeScript v5.9.」
ずんだもん「Pick what fits the situation!」

