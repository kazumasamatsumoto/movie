# #455「デバッグ(2)」

四国めたん「void関数の高度なデバッグについて学びましょう！」
ずんだもん「他のデバッグ方法はあるの？」
四国めたん「TypeScriptのデバッガ、テスト、モックを使います。副作用を検証できますね」
ずんだもん「どうやって検証するの？」
四国めたん「スパイ関数で呼び出しを記録します。Jestやsinon.jsが便利ですね」
ずんだもん「なるほど！テストで確認するんだね！」
四国めたん「副作用の発生を assert で確認します。TypeScript v5.9でも有効な手法ですね」
ずんだもん「テスト駆動で開発できるんだね！」

---

## 📺 画面表示用コード

```typescript
// スパイ関数でのテスト
import { jest } from '@jest/globals';

test('processData calls database.save', () => {
  const spy = jest.spyOn(database, 'save');
  processData(mockData);
  expect(spy).toHaveBeenCalledWith(mockData);
});
```

```typescript
// 副作用の検証
test('update increments counter', () => {
  let counter = 0;
  function increment(): void {
    counter++;
  }
  increment();
  expect(counter).toBe(1);
});
```

```typescript
// モックを使った検証
const mockLogger = {
  log: jest.fn()
};
function process(): void {
  mockLogger.log('Processing');
}
process();
expect(mockLogger.log).toHaveBeenCalledWith('Processing');
```
