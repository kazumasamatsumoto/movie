# #387「console.logの戻り値」

四国めたん「console.logの戻り値について学びましょう！」
ずんだもん「console.logって戻り値あるの？」
四国めたん「型定義は void です。実行時は undefined を返しますが、戻り値を使うべきではありませんね」
ずんだもん「なぜvoid型なの？」
四国めたん「ログ出力は副作用のための処理です。戻り値を期待しないことを型で表現していますね」
ずんだもん「なるほど！戻り値を使わないんだね！」
四国めたん「他のconsoleメソッドも同様です。TypeScript v5.9でも同じ型定義ですね」
ずんだもん「副作用関数の典型例なんだね！」

---

## 📺 画面表示用コード

```typescript
// console.logの型定義
// console.log(message?: any, ...optionalParams: any[]): void
const result = console.log("Hello");
// result: void型
```

```typescript
// 戻り値を使うべきでない
function logAndReturn(msg: string): string {
  return console.log(msg);  // エラー: Type 'void' is not assignable to type 'string'
}
```

```typescript
// 正しい使い方
function process(data: Data): void {
  console.log("Processing:", data);
  // 副作用のみ
}
```
