# #377「void型変数」

四国めたん「void型の変数について学びましょう！」
ずんだもん「void型の変数って作れるの？」
四国めたん「はい、作れますが実用性は低いです。undefined しか代入できませんね」
ずんだもん「どんな時に使うの？」
四国めたん「ほとんど使いません。関数の戻り値型としての使用が主です。変数としては意味がないですね」
ずんだもん「なるほど！あまり使わないんだね！」
四国めたん「strictNullChecks有効時は undefined のみ代入可能です。TypeScript v5.9でも変わりませんね」
ずんだもん「関数の型として使うのが基本なんだね！」

---

## 📺 画面表示用コード

```typescript
// void型変数 (実用性は低い)
let value: void;
value = undefined;  // OK
// value = null;    // strictNullChecks有効時はエラー
```

```typescript
// 関数の戻り値として使うのが一般的
function execute(): void {
  console.log("Executed");
}
const result: void = execute();
```

```typescript
// 実用的な使用例
type VoidCallback = () => void;
const callbacks: VoidCallback[] = [];
callbacks.push(() => console.log("Done"));
```
