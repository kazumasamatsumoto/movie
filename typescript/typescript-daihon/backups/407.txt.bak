# #407「戻り値型がundefined」

四国めたん「戻り値型がundefinedの関数について学びましょう！」
ずんだもん「undefinedを戻り値型にする意味は？」
四国めたん「明示的に undefined 値を返すことを示します。値として扱う意図ですね」
ずんだもん「void型とどう違うの？」
四国めたん「void は戻り値を無視、undefined は undefined という値を返します。意味が異なりますね」
ずんだもん「なるほど！値として使うんだね！」
四国めたん「通常は T | undefined の形式で使います。単独で使うことは稀ですね」
ずんだもん「ユニオン型で使うのが基本なんだね！」

---

## 📺 画面表示用コード

```typescript
// undefined戻り値型
function getOptionalValue(): number | undefined {
  if (Math.random() > 0.5) {
    return 42;
  }
  return undefined;  // 明示的にundefinedを返す
}
```

```typescript
// 値としてチェック
const value = getOptionalValue();
if (value !== undefined) {
  console.log(value * 2);
}
```

```typescript
// void型との違い
function voidFunc(): void {
  console.log("Done");
  // 戻り値を使うべきでない
}
function undefFunc(): undefined {
  return undefined;  // 値として返す
}
```
