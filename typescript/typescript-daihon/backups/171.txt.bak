# #171 「変換失敗時 - NaN」台本

四国めたん「TypeScript v5.9の変換失敗時について学びましょう！」
ずんだもん「変換失敗時って何？」
四国めたん「数値変換に失敗した時にNaNが返されることです」
ずんだもん「どんな場合に失敗するの？」
四国めたん「無効な文字列や変換不可能な値の場合です」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、エラーハンドリングに重要です」
ずんだもん「エラーハンドリングで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 変換失敗時 - NaN
let invalidStr: string = "abc";
let emptyStr: string = "";

// 変換失敗
let result1: number = Number(invalidStr);  // NaN
let result2: number = parseInt(invalidStr); // NaN
let result3: number = parseFloat(emptyStr); // NaN

// 実用的な例
let userInput: string = "無効な値";
let userAge: number = Number(userInput);

if (Number.isNaN(userAge)) {
  console.log("無効な数値です");
} else {
  console.log("有効な数値です");
}
```
