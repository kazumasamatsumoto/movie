# #335「nullableオブジェクト」

四国めたん「nullableオブジェクトについて学びましょう！」
ずんだもん「オブジェクトがnullableってどう扱うの？」
四国めたん「User | null のようにオブジェクト全体がnullの可能性がある型です。データ検索で頻繁に使いますね」
ずんだもん「プロパティアクセスはどうするの？」
四国めたん「Optional Chainingでプロパティに安全にアクセスできます。user?.name のように書きますね」
ずんだもん「なるほど！安全にアクセスできるんだね！」
四国めたん「型ガードでnullチェック後は通常のオブジェクトとして扱えます。型安全性が保証されますね」
ずんだもん「型でnullを安全に扱えて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// nullableオブジェクト
function findUser(id: number): User | null {
  return users.find(u => u.id === id) ?? null;
}
```

```typescript
// Optional Chainingでアクセス
const user = findUser(1);
const name = user?.name;
const age = user?.age ?? 0;
```

```typescript
// 型ガードで安全に
if (user !== null) {
  console.log(user.name); // User型
  console.log(user.age);
}
```
