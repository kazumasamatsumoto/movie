# #161 「Numberからnumberへ」台本

四国めたん「TypeScript v5.9のNumberからnumberへの変換について学びましょう！」
ずんだもん「Numberからnumberへの変換って何？」
四国めたん「Numberオブジェクトをプリミティブのnumber型に変換する方法です」
ずんだもん「どうやって変換するの？」
四国めたん「valueOf()メソッドやNumber()関数を使用します」
ずんだもん「Angularの開発でも使える？」
四国めたん「はい、データ変換や型の統一に活用できます」
ずんだもん「型変換で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// Numberからnumberへの変換

// 1. valueOf()メソッド
// let numObj = new Number(30);
// let num: number = numObj.valueOf(); // number型に変換

// 2. Number()関数
// let numObj = new Number(30);
// let num: number = Number(numObj); // number型に変換

// 3. 数値リテラル（推奨）
let num: number = 30; // 最初からnumber型

// 実用的な例
let userData = {
  age: 25,
  price: 1500
};

let userAge: number = Number(userData.age);
let productPrice: number = Number(userData.price);
```
