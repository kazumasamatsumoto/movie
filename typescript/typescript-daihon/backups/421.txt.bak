# #421「await」

四国めたん「Promise<void>のawaitについて学びましょう！」
ずんだもん「Promise<void>をawaitするとどうなるの？」
四国めたん「処理の完了を待ちます。await式の結果は void 型ですね」
ずんだもん「値は取れないの？」
四国めたん「はい、値はありません。完了を待つだけです。その後の処理を続行できますね」
ずんだもん「なるほど！順次実行できるんだね！」
四国めたん「複数のPromise<void>を順番に待てます。TypeScript v5.9でも基本的な使い方ですね」
ずんだもん「非同期処理を制御できるんだね！」

---

## 📺 画面表示用コード

```typescript
// awaitで完了を待つ
async function process(): Promise<void> {
  await saveData(data);
  await logActivity("Saved");
  console.log("All done");
}
```

```typescript
// await式の型はvoid
async function example(): Promise<void> {
  const result: void = await initialize();
  // resultは使わない
}
```

```typescript
// 複数の処理を順次実行
async function sequence(): Promise<void> {
  await step1();
  await step2();
  await step3();
}
```
