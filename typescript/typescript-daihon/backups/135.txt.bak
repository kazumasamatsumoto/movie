# #135 「特殊な数値まとめ」台本

四国めたん「TypeScript v5.9の特殊な数値まとめについて学びましょう！」
ずんだもん「特殊な数値まとめって何？」
四国めたん「Infinity、NaN、判定関数の要点をまとめたものです」
ずんだもん「どんな要点があるの？」
四国めたん「型の統一、適切な判定、ベストプラクティスなどがあります」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、数値処理の理解に重要です」
ずんだもん「特殊数値で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 特殊な数値まとめ

// 1. 特殊な数値
let infinity: number = Infinity;
let negativeInfinity: number = -Infinity;
let nan: number = NaN;

// 2. 判定関数
let isNan: boolean = Number.isNaN(nan);
let isFinite: boolean = Number.isFinite(infinity);
let isInteger: boolean = Number.isInteger(42);
let isSafe: boolean = Number.isSafeInteger(123);

// 3. 実用的な例
let userInput: number = parseFloat("123.45");
if (Number.isFinite(userInput) && Number.isSafeInteger(userInput)) {
  console.log("有効な整数です");
}
```