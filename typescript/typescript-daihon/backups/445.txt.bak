# #445「RxJSオペレータ」

四国めたん「RxJSオペレータとvoid型について学びましょう！」
ずんだもん「どのオペレータでvoidを使うの？」
四国めたん「tap()、finalize()、forEach()などで使います。副作用のための処理ですね」
ずんだもん「どう書くの？」
四国めたん「コールバック関数の戻り値型を void にします。値は返しませんね」
ずんだもん「なるほど！副作用専用なんだね！」
四国めたん「デバッグやロギングに便利です。TypeScript v5.9でも頻繁に使われますね」
ずんだもん「開発で役立つんだね！」

---

## 📺 画面表示用コード

```typescript
// tap()オペレータ
import { tap } from 'rxjs/operators';

data$.pipe(
  tap((data): void => {
    console.log('Data:', data);
  }),
  tap((): void => {
    console.log('Processing');
  })
).subscribe();
```

```typescript
// finalize()オペレータ
import { finalize } from 'rxjs/operators';

request$.pipe(
  finalize((): void => {
    console.log('Request complete');
    cleanup();
  })
).subscribe();
```

```typescript
// forEach()メソッド
users$.forEach((user): void => {
  console.log('User:', user.name);
});
```
