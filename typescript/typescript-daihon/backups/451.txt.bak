# #451「間違い(1) - 戻り値使用」

四国めたん「void型の間違いについて学びましょう！」
ずんだもん「どんな間違いがあるの？」
四国めたん「void関数の戻り値を使おうとすることです。型エラーになりますね」
ずんだもん「具体的にどういうこと？」
四国めたん「void関数の結果を変数に代入したり、計算に使うとエラーです。戻り値は使えませんね」
ずんだもん「なるほど！値がないから使えないんだね！」
四国めたん「void型は戻り値を無視する契約です。TypeScript v5.9でも型チェックで防げますね」
ずんだもん「型システムが守ってくれるんだね！」

---

## 📺 画面表示用コード

```typescript
// 間違い: void戻り値を使用
function log(msg: string): void {
  console.log(msg);
}
const result: string = log("Hello");  // エラー
```

```typescript
// 間違い: 計算に使用
function update(): void {
  count++;
}
const value = update() + 1;  // エラー
```

```typescript
// 正しい使い方
function process(): void {
  doSomething();
}
process();  // 戻り値を使わない
console.log("Done");
```
