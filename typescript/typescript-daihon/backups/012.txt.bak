# #012 「シングルクォート文字列 - 'hello'」台本

四国めたん「TypeScript v5.9のシングルクォート文字列について学びましょう！」
ずんだもん「シングルクォート文字列って何？」
四国めたん「'で囲まれた文字列リテラルです」
ずんだもん「ダブルクォートと何が違うの？」
四国めたん「機能的には同じですが、プロジェクトで統一することが重要です」
ずんだもん「Angularの開発でも使える？」
四国めたん「はい、一貫性のあるコードスタイルに活用できます」
ずんだもん「シングルクォートで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// シングルクォート文字列
let message: string = 'Hello, World!';
let name: string = 'Alice';
let description: string = 'TypeScript学習中';

// 実用的な例
let apiUrl: string = 'https://api.example.com';
let errorMessage: string = 'エラーが発生しました';
let successMessage: string = '処理が完了しました';

// 文字列の結合
let greeting: string = 'こんにちは、' + name + 'さん！';
let fullMessage: string = message + ' ' + description;
```