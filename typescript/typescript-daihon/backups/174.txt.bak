# #174 「型安全な変換」台本

四国めたん「TypeScript v5.9の型安全な変換について学びましょう！」
ずんだもん「型安全な変換って何？」
四国めたん「TypeScriptの型システムを活用した安全な変換方法です」
ずんだもん「どうやって実現するの？」
四国めたん「型ガードやユニオン型を使用します」
ずんだもん「Angularの開発でも重要？」
四国めたん「はい、型安全性を保ちながら変換に重要です」
ずんだもん「型安全変換で最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 型安全な変換
function isNumber(value: unknown): value is number {
  return typeof value === "number" && Number.isFinite(value);
}

function safeConvert(value: unknown): number | null {
  if (isNumber(value)) {
    return value;
  }
  
  if (typeof value === "string") {
    let converted = Number(value);
    return Number.isNaN(converted) ? null : converted;
  }
  
  return null;
}

// 実用的な例
let userInput: unknown = "25";
let userAge: number | null = safeConvert(userInput);

if (userAge !== null) {
  console.log(`年齢: ${userAge}`);
}
```
