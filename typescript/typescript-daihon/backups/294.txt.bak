# #294「nullのパターン」

四国めたん「nullの実践的なパターンについて学びましょう！」
ずんだもん「nullってどんなパターンで使うの？」
四国めたん「Null Objectパターン、Option型、Repository検索、ファクトリーメソッドなど、様々なデザインパターンで活用されます」
ずんだもん「具体的なコード例を教えて！」
四国めたん「型ガードと組み合わせたnullチェック、デフォルト値設定、Result型でのエラー処理などが代表的ですね」
ずんだもん「なるほど！実践的なパターンがあるんだね！」
四国めたん「Angularのサービスやインジェクション、Nest.jsのRepositoryでも頻繁に使われますね」
ずんだもん「デザインパターンで型安全に実装できて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// Repository パターン
class UserRepository {
  findById(id: number): User | null {
    return this.users.find(u => u.id === id) ?? null;
  }
}
```

```typescript
// Option型パターン
type Option<T> = T | null;
function safeDivide(a: number, b: number): Option<number> {
  return b !== 0 ? a / b : null;
}
```

```typescript
// Null Objectパターン
const user = findUser(id) ?? createGuestUser();
if (user !== null) {
  console.log(user.name);
}
```
