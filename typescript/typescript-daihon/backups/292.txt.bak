# #292「nullと空文字列」

四国めたん「nullと空文字列の違いについて学びましょう！」
ずんだもん「nullと空文字列って何が違うの？」
四国めたん「nullは値がないことを示し、空文字列''は長さ0の文字列です。型も意味も異なります」
ずんだもん「どっちも空っぽに見えるけど...」
四国めたん「空文字列は有効な文字列値です。Nullish Coalescingでは空文字列は有効値として扱われます」
ずんだもん「なるほど！空文字列は存在する値なんだね！」
四国めたん「型安全性により、string型にnullは代入できません。string | null が必要ですね」
ずんだもん「型システムで明確に区別できて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// 型の違い
let a: null = null;     // null型
let b: string = "";     // string型
null === "";  // false
```

```typescript
// Nullish Coalescingの動作
const name1 = null ?? "Guest";  // "Guest"
const name2 = "" ?? "Guest";    // "" (空文字列は有効値)
```

```typescript
// 型安全性
let str: string = null;  // エラー
let str: string | null = null;  // OK
```
