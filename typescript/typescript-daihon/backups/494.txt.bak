# #494「使用例」

四国めたん「無限ループの実践的な使用例について学びましょう！」
ずんだもん「実際にどう使われてるの？」
四国めたん「WebSocketサーバー、タスクキュー、監視プロセスなどで使われます」
ずんだもん「具体的にどんなコード？」
四国めたん「リクエストを待ち続ける、メッセージを処理し続ける、状態を監視し続けるパターンですね」
ずんだもん「なるほど！常駐プログラムなんだね！」
四国めたん「Node.jsやサーバーアプリで見られます。TypeScript v5.9でも同じパターンですね」
ずんだもん「バックエンド開発で使うんだね！」

---

## 📺 画面表示用コード

```typescript
// WebSocketサーバー
function runWebSocketServer(): never {
  const server = createServer();
  while (true) {
    const connection = server.accept();
    handleConnection(connection);
  }
}
```

```typescript
// タスクキュー処理
function processQueue(): never {
  while (true) {
    const task = queue.dequeue();
    if (task) {
      executeTask(task);
    } else {
      sleep(100);
    }
  }
}
```

```typescript
// 監視プロセス
function watchFiles(): never {
  while (true) {
    const changes = detectChanges();
    if (changes.length > 0) {
      handleChanges(changes);
    }
    await delay(1000);
  }
}
```
