# #481「使用例」

四国めたん「never型の実践的な使用例について学びましょう！」
ずんだもん「実際にどう使われてるの？」
四国めたん「バリデーション、アサーション、エラーハンドラで使われます」
ずんだもん「具体的にどんなコード？」
四国めたん「入力チェック、前提条件の確認、到達不可能コードの検出などですね」
ずんだもん「なるほど！実用的なんだね！」
四国めたん「ライブラリやフレームワークでも頻繁に使われます。TypeScript v5.9でも標準パターンですね」
ずんだもん「いろんな場面で活躍するんだね！」

---

## 📺 画面表示用コード

```typescript
// バリデーション
function validatePositive(value: number): void {
  if (value <= 0) {
    throwError("Value must be positive");
  }
}

function throwError(message: string): never {
  throw new Error(message);
}
```

```typescript
// アサーション
function assertDefined<T>(value: T | undefined): asserts value is T {
  if (value === undefined) {
    throw new Error("Value is undefined");
  }
}
```

```typescript
// エラーハンドラ
function handleError(error: unknown): never {
  console.error("Fatal error:", error);
  process.exit(1);
}
```
