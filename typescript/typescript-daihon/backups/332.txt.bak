# #332「Nullish Coalescing - x ?? default」

四国めたん「Nullish Coalescingについて学びましょう！」
ずんだもん「?? ってどんな機能なの？」
四国めたん「nullまたはundefinedの時だけデフォルト値を返す演算子です。0や空文字列は有効値として扱いますね」
ずんだもん「|| との違いは？」
四国めたん「|| は全てのfalsyな値でデフォルト値を返しますが、?? はnullとundefinedのみです」
ずんだもん「なるほど！厳密にnullチェックできるんだね！」
四国めたん「nullable型と相性が良く、デフォルト値設定に最適です。TypeScript v5.9で型推論も強化されていますね」
ずんだもん「型安全にデフォルト値を設定できて最高だよ！」

---

## 📺 画面表示用コード

```typescript
// Nullish Coalescing
const name = userName ?? "Guest";
const port = config.port ?? 3000;
```

```typescript
// || との違い
const count1 = 0 || 10;  // 10 (0はfalsy)
const count2 = 0 ?? 10;  // 0  (0は有効値)
```

```typescript
// Optional Chainingと組み合わせ
const city = user?.address?.city ?? "Unknown";
const age = user?.age ?? 18;
```
