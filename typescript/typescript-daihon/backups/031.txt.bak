# #031 「複数行文字列の実例 - HTMLテンプレート」台本

四国めたん「TypeScript v5.9の複数行文字列実例について学びましょう！」
ずんだもん「HTMLテンプレートってどう書くの？」
四国めたん「テンプレートリテラルでHTMLをそのまま記述できます」
ずんだもん「どんなHTMLでも使えるの？」
四国めたん「はい、複雑なHTML構造も簡単に記述できます」
ずんだもん「Angularの開発でも使える？」
四国めたん「はい、動的なHTML生成に活用できます」
ずんだもん「HTMLテンプレートで最高だよ！」

---

## 📺 画面表示用コード

```typescript
// HTMLテンプレートの例
let product = {
  name: "TypeScript学習本",
  price: 2980,
  description: "TypeScriptの基礎から応用まで"
};

// HTMLテンプレート
let productCard: string = `
<div class="product-card">
  <h3 class="product-title">${product.name}</h3>
  <p class="product-description">${product.description}</p>
  <div class="product-price">¥${product.price}</div>
  <button class="add-to-cart">カートに追加</button>
</div>
`;

// 実用的な例
let user = { name: "Alice", role: "admin" };
let userProfile: string = `
<div class="profile">
  <h2>${user.name}</h2>
  <span class="role-badge">${user.role}</span>
</div>
`;
```