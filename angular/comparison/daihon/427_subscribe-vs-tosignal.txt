# #427 「RxJS subscribeでUI同期 vs toSignalブリッジ あなたはどっち派？」台本

ずんだもん「Observableの結果をUIに渡すとき、`.subscribe()`で代入？`toSignal`で変換？」
四国めたん「Signals登場でObservableとテンプレートの接続方法が増えました。」
ずんだもん「subscribeして代入する利点は？」
四国めたん「手元でデータを加工しやすく、古いコードとも相性抜群。`AsyncPipe`を使わなくても任意タイミングで更新できます。」
ずんだもん「でも購読解除を忘れるとリークするし、`ChangeDetectorRef`を触ることも…。」
四国めたん「`toSignal(observable$, { initialValue })`でSignalへ変換でき、テンプレートでも`{{ data() }}`と呼べます。`takeUntilDestroyed`不要です。」
ずんだもん「`effect`に`toSignal`を渡すとコードの意図が明快になるね。」
四国めたん「一時的ならsubscribe、本格的にはtoSignalで統一するのがおすすめ。あなたはどっち派？」
