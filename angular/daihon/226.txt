# #226 「動的 Component への入力渡し」台本

ずんだもん「動的に生成したコンポーネントにデータを渡したいんだけど!」
四国めたん「ComponentRef.setInput()メソッドを使えば入力値を設定できます」
ずんだもん「instance経由で直接代入する方法もあるよね?」
四国めたん「はい、ただしv14以降はsetInput()の使用が推奨されています」
ずんだもん「Signal入力にも対応してるの?」
四国めたん「はい！setInput()はSignal入力にも完全対応しており、型安全に値を渡せます」
ずんだもん「これで動的コンポーネントにも安全にデータを渡せるね!」
