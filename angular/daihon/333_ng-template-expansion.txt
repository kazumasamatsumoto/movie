# #333 「<ng-template> への展開」台本

ずんだもん「構造ディレクティブが<ng-template>に展開されるってどういうこと？」
四国めたん「Angularコンパイラが裏側で`<ng-template [ngIf]=...>`のように書き換えてくれます。」
ずんだもん「テンプレートに渡される値はどこで管理されてるの？」
四国めたん「ディレクティブの`TemplateRef`と`ViewContainerRef`が受け取ってビューを生成します。」
ずんだもん「仕組みを知っておくとカスタムディレクティブを作るときに役立つね。」
