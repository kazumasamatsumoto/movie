# #098 「@Input() オブジェクトの受け渡し」

ずんだもん「オブジェクトを渡す方法を教えて！」
四国めたん「@Input() user: Userのようにオブジェクト型を指定し、親で[user]="userObject"と渡します」
ずんだもん「参照渡しになるの？」
四国めたん「はい！オブジェクトは参照渡しなので、子で変更すると親にも影響します」
ずんだもん「変更を防ぎたい場合はどうするの？」
四国めたん「Object.freeze()やスプレッド演算子で不変性を保つか、Readonly型を使用します」
