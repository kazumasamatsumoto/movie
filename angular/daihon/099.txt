# #099 「@Input() 配列の受け渡し」

ずんだもん「配列を渡す方法を教えて！」
四国めたん「@Input() items: Item[]のように配列型を指定し、親で[items]="itemArray"と渡します」
ずんだもん「配列も参照渡しになるの？」
四国めたん「はい！配列も参照渡しなので、子で変更すると親の配列も変更されます」
ずんだもん「安全に使うにはどうするの？」
四国めたん「[...items]でスプレッド演算子を使うか、ReadonlyArray型を使用して不変性を保ちます」
