# #377 「配列でのクラス指定」台本

ずんだもん「配列でクラスを指定すると何が便利？」
四国めたん「複数クラスをリストで渡せるので、状態に応じて簡単に増減できます。」
ずんだもん「条件付きで要素を入れたり外したりもできる？」
四国めたん「`[ngClass]=\"[baseClass, isActive ? 'is-active' : '']\"`のように書けます。」
ずんだもん「テンプレートが読みやすくなるね。」
