# #580 「*ngIf=\"data$ | async as data\"」台本

ずんだもん「実際にどう書くの？」
四国めたん「`<ng-container *ngIf=\"user$ | async as user\">{{ user.name }}</ng-container>`のようにします。」
ずんだもん「nullの時は表示されない？」
四国めたん「はい、値がある時だけ描画されます。」
