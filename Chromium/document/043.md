# #043 「CompositorとMain Threadの分離」

```text
Component Flow
Main Thread -> Compositor Frame Sink -> GPU
```

```bash
chrome://tracing -> track:RendererCompositorThread
```

```text
Input events: HitTest -> Compositor -> (必要なら) Main ThreadへFallback
```
