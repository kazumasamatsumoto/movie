# #040 「60FPSを実現するフレームバジェット」

```bash
chrome://tracing -> category:devtools.timeline,renderer.scheduler
```

```text
フレームバジェット
- 16.6ms = Script + Style + Layout + Paint + Composite
- Idleタスクは予算外で実行
```

```bash
chrome://flags/#enable-frame-throttling-debug
```
